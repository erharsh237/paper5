{
  "name": "functions",
  "description": "Server-side AI Assistant backend — holds the Anthropic API key so it never reaches the browser.",
  "type": "module",
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
